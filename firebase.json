{
  "database": {
    "rules": "database.rules.json"
  },
  // FIX: HTTP Error: 400, Project 'datcom-115d9' is not a Firestore enabled project.
  // "firestore": {
  //   "rules": "firestore.rules",
  //   "indexes": "firestore.indexes.json"
  // },
  "functions": {
    "predeploy": [
      "npm --prefix \"$RESOURCE_DIR\" run lint"
    ]
  },
  "storage": {
    "rules": "storage.rules"
  }
}
